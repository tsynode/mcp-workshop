# Use a newer version of Streamlit that's compatible with protobuf 5.x
streamlit==1.31.0

# Core dependencies
boto3==1.37.21
botocore==1.37.23
awscli>=1.32.0
requests==2.31.0
python-dotenv==1.0.0

# MCP dependencies
mcp==1.6.0
pydantic==2.10.2

# Explicitly set protobuf to a version compatible with both Streamlit and OpenTelemetry
protobuf>=3.20.0,<6.0.0

# Disable OpenTelemetry to avoid dependency conflicts
INLINEAGENT_DISABLE_TELEMETRY=true

# Install InlineAgent directly from GitHub with minimal dependencies
git+https://github.com/awslabs/amazon-bedrock-agent-samples.git#subdirectory=src/InlineAgent
